
```bash
pkill -f rstudio -9

ps aux | grep -i rstudio
pidof nginx
pgrep ngin
kill -9 pid

killall -u kong
killall -9 firefox

# If you want to kill processes interactively, you can use -i like so:
killall -i firefox
```